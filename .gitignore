build/
build-bundle/
dist/
compile_commands.json
.DS_Store

# Android. build/ above already covers android/app/build.
android/.gradle/
android/app/.cxx/
android/local.properties

# Signing. The keystore is a private key and the passwords are secrets; neither
# belongs in a repository. .github/signing.env.example shows the shape of the
# second without holding anything itself.
android/app/*.jks
.github/signing.env

# rfxgen project files, the wavs are what the game needs
*.rfx

# internal notes
INTERNAL.md

# editor leftovers
.*.swp
.*.swo
*~
